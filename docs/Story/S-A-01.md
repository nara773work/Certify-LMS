チケットID：S-A-01

GoogleカレンダーとLMSを連携させることで外部との予定で面談が実施できない時刻の面談スケジュールを非表示にできる

以下の手順でGoogleカレンダーを連携させる
- Google Calendar APIを有効にする
- OAuthスコープを設定する(http://localhost:8000/auth/google/callbackがリダイレクト先URL)
- .envファイルにIDとシークレットを設定する
- 以下のコマンドでライブラリを入れる
```bash
composer require google/apiclient
```
- コントローラー、ルートを作成する
# 実装内容
- 面談設定タブから自分の Google アカウントを連携できる
- 連携はいつでも解除できる
- 連携済コーチが Google カレンダーで予定を持つ時刻は、受講生の予約画面の空き枠から外れる
- 連携していないコーチは従来通りの空き判定で動く
- 連携済コーチが担当する面談が成立すると、そのコーチの Google カレンダーへ面談の予定が自動登録される
- 連携していないコーチには登録しない
- その面談がキャンセルされると、登録済の予定も連動して削除される

# シーディング
|コーチ名|連携有無|
|コーチ太郎|連携済み|
|コーチ花子|未連携|

# テーブル設計
Schema::create('google_calendar_tokens', function (Blueprint $table) {
    $table->id();
    $table->foreignUlid('user_id')
        ->constrained('users')
        ->cascadeOnDelete();
    $table->string('access_token');
    $table->string('refresh_token');
    $table->timestamp('expires_at');
    $table->timestamp('refresh_token_expires_at');
    $table->timestamps();
});

# test
- 設定画面の面談設定タブから自分の Google アカウントを連携できる 1
- 連携はいつでも解除できる 2
- 連携済コーチが Google カレンダーで予定を持つ時刻は、受講生の予約画面の空き枠から外れる 3
- 連携していないコーチは従来通りの空き判定で動く 4
- 連携済コーチが担当する面談が成立すると、そのコーチの Google カレンダーへ面談の予定が自動登録される 5
- 連携していないコーチには登録しない 6
- その面談がキャンセルされると、登録済の予定も連動して削除される 7

tests/Feature/
├── Http/
    ├── GoogleCalendarControllerTest.php 1,2
    └── Meeting/
        ├── MeetingAvailabilityTest.php 3,4
        └── MeetingControllerTest.php 5,6,7
